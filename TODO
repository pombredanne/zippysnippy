- If snippet does not end in a period, a period should be added.
- <e></e> is used for editor's notes
- <h></h> is used to hide former versions or old text
  - Length calculations should not take hidden text into account
- ADD mode: toggle that only displays short entries
- support for external files
- optional speedup parser written in C
- temporarily "ban" categories
  - actually, i was thinking something like checkmarks and X's in the menu
  - then you can have a key enable one category and change all others to X's,
    one key to X a specific category, etc
- Look into vim remoting, or keeping it awake so we don't incur startup cost
- "about 1 weeks ago" in date snipped area
- color quoted text that starts with >
- fix: "1 are ready for review (footer)"
- snippets that reference specific files. maybe use symbolic links so the
  actual files can be moved
- 'p' should reverse 'S'
- Make category a drop-down menu
- Unmark: don't mark current item as read
- Write to disk more frequently
- QuickStart screen for first run without any data.
- Bulk source verify: recheck all sources, mark bad ones
- Clipboard:
  - detect pdf viewer vs web browser
  - it's probably possible to get the source application for the selection in
    the X clipboard by going through the C api but its sortof a pain. take a
    look at the xclip source code for examples, if you care enough to
  - see also http://tronche.com/gui/x/icccm/
